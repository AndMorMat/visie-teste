import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  buttonCancelar: {
    backgroundColor: '#933'
  },
  textCancelar: {
    color: '#fff'
  },
  buttonContainer: {
    flexDirection: 'row', 
    marginTop: 10
  }
})

export default styles