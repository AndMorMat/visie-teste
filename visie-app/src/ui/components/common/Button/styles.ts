import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  defaultButton: {
    flex: 1,
    borderWidth: 0.5,
    padding: 10,
    borderRadius: 5,
  },
  defaultButtonText: {
    textAlign: 'center',
  }

})

export default styles